import React, { FunctionComponent } from "react";

interface EditProps {
    id:number
}
 
const Edit: FunctionComponent<EditProps> = (props) => {
    return ( <div>
        hello world
    </div> );
}
 
export default Edit;