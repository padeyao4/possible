import type { PiniaPluginContext } from 'pinia';

export function piniaPersisted({ store }: PiniaPluginContext) {
  /*  store.$onAction((params) => {
    const { store, name } = params;
    console.log(store.$id, name);
  }, true);*/
  /*  store.$subscribe((params) => {
  });*/
}
